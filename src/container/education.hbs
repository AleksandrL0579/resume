<div>
    {{#with main}}

    <main>
        <section>
            {{>title data='My educations'big=true }}
            <ul>
                {{#each educations}}
                <li class="my-3">
                    {{name}}
                    {{#unless isEnd}} (ще в процесi) {{/unless}}
                </li>
                {{/each}}
            </ul>
        </section>
        <section>
            {{>title data='My certificates' }}
            <ul>
                {{#each certificates}}
                <li class="my-3">
                    {{name}} ({{id}})

                </li>
                {{/each}}
            </ul>
        </section>
    </main>

    {{/with}}

</div>